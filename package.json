{
  "name": "ring-vocabulary",
  "version": "1.0.0",
  "description": "画像から単語を抽出し、単語帳学習ができるアプリ",
  "private": true,
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:api": "cd api && npm run dev",
    "dev": "concurrently \"npm run dev:api\" \"npm run dev:frontend\"",
    "build:frontend": "cd frontend && npm run build",
    "build:api": "cd api && npm run build",
    "build": "npm run build:api && npm run build:frontend",
    "vercel-build": "npm install && cd api && npm install && npm run build && mkdir -p ../api/src && cp -r dist/* ../api/src/ && cd ../frontend && npm install && npm run build",
    "start": "node api/src/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
